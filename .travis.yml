language: objective-c
before_install:
    - gem install ocunit2junit
    - export LC_ALL="en_US.UTF-8"
before_script:
    - cd macvim/src
    - ./configure
    - make
    - cd ../..
script: xcodebuild -workspace VimR.xcworkspace -scheme VimR -derivedDataPath build/VimR -configuration Debug build test

