language: objective-c
osx_image: xcode9

git:
    depth: 1

cache:
    directories:
        - Carthage

before_install: brew update && brew bundle

script:
    - brew bundle exec ./bin/build_travis.sh
